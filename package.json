{
  "name": "campaign-builder",
  "version": "1.4.1",
  "description": "A self-contained suite for creating and managing the world of a D&D (or equivalent) campaign.",
  "main": "src/main.js",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "coverage": "node --test --experimental-test-coverage --test-coverage-exclude='tests/**' tests/*.test.js",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "bench": "node bench/app-bench.js",
    "bench:pure": "node bench/pure-bench.js",
    "build": "node scripts/build.cjs",
    "dev": "node scripts/build.cjs --watch",
    "deploy": "bash scripts/deploy.sh",
    "guide": "node scripts/build-dev-guide.mjs",
    "guide:check": "node scripts/build-dev-guide.mjs --check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.17.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "eslint": "^10.8.0",
    "typescript": "^7.0.2"
  }
}
